%h3 Users
.col-span-8
%table.table.table-condensed
  %thead 
    %tr 
      %th Username
      %th Email
      %th Registered
      %th Role
      %th 
      %th
  %tbody 
  - @users.each do |user|
    %tr 
      %td= link_to user.name, user
      %td= link_to user.email
      %td= link_to user.created_at.to_date
      %td= link_to user.roles.first.name.titleize unless user.roles.first.nil?
      %td
        %a.btn.btn-xs.btn-primary{'data-toggle'=>"modal", href:"#role-options-#{user.id}"} Change a role
        = render user
      %td= link_to("Delete user", user_path(user), data: {confirm: "Are you sure?"}, method: :delete, class: 'btn btn-mini') unless user == current_user